# oss-audit report

Generated by `oss-audit` against commit `41f3df930cd04f2ba4ef5ed80a11b67cca77c1ea`
(`main`), scored against `STANDARD.md` at 61 rules.

Re-scored after the fix work on branch `chore/oss-audit-fixes`. Verdicts below
read the working tree of that branch and the forge settings as they stand after
the writes it made, not the state at the commit named above. The original run
found 14 fails; 12 are closed, 2 stay open until the next `v*` tag publishes a
release, and R-SEC-14 moved from not applicable to pass because the repository
now has an updater configuration for it to score.

This file is untracked by default. Commit it if you want the diff on the next
audit, or delete it once the fix groups are done.

## Repository

- Forge: GitHub, `svyatov/handrail`, public, not archived.
- Shipped ecosystems: Go modules (`go.mod` declares `github.com/svyatov/handrail`,
  tags `v0.1.0` and `v0.1.0-rc.1` exist and resolve through `proxy.golang.org`).
  Release track: tag-published.
- Present ecosystems: Go modules only. No `package.json`, `pyproject.toml`,
  `Gemfile`, `Cargo.toml`, `composer.json`, `mix.exs`, `pubspec.yaml`, or project
  file for NuGet or Maven anywhere in the checkout. No workflow pushes a
  container image, so containers are absent rather than present.
- Lockfile: `go.sum`, committed, three modules, all test-only or indirect.
  `go list -deps` reports zero non-stdlib runtime dependencies.
- CI configuration: `.github/workflows/ci.yml` (jobs `test`, `lint`,
  `release-config`) and `.github/workflows/release.yml` (job `release`). No
  `action.yml` or `action.yaml` is shipped. Every CI step runs a `Taskfile.yml`
  task.
- Release today: pushing a `v*` tag runs `.github/workflows/release.yml`, which
  runs GoReleaser (`.goreleaser.yml`) to build darwin and linux binaries on amd64
  and arm64, upload four `tar.gz` archives plus `checksums.txt` to the GitHub
  release, and push a Homebrew cask to `svyatov/homebrew-tap` using the
  `HOMEBREW_TAP_TOKEN` secret. Tags are annotated and SSH-signed; the signing key
  is published at `users/svyatov/ssh_signing_keys`.
- Agent skills: `skills/add/` and `skills/analyze/`, each one `SKILL.md`, no
  scripts, no per-skill manifests. Plugin manifests at
  `.claude-plugin/plugin.json`, `.claude-plugin/marketplace.json`, and
  `.codex-plugin/plugin.json`.
- Access: one principal (`svyatov`, admin) holds every merge path.
- Forge settings, read back after the fix work wrote them: three active
  rulesets, 21093767 `Default branch is reviewed and green` (branch, no bypass
  actors), 21093604 `Only an admin creates a release tag` (tag, `creation`,
  bypass actor `RepositoryRole:5`), and 21094237 `Release tags never move` (tag,
  `update` and `deletion`, no bypass actors). The two tag rulesets are separate
  because GitHub scopes a bypass actor to the whole ruleset, so one ruleset
  cannot both let the admin create a tag and stop the admin moving it.
  `secret_scanning`, `secret_scanning_push_protection`, and
  `dependabot_security_updates` all enabled; `vulnerability-alerts` answers 204;
  `code-scanning/default-setup` reports `configured` for languages `actions` and
  `go`; private vulnerability reporting enabled; Discussions disabled. Classic
  branch protection still answers `404 Branch not protected`, which is what
  GitHub returns when a ruleset rather than a classic rule guards the branch.
- CI check names, for anyone editing the branch ruleset: `test (ubuntu-latest)`,
  `test (macos-latest, SHORT=-short)`, `lint`, `release-config`, `Analyze (go)`,
  `Analyze (actions)`. All six are required. `Adjust Configuration` and
  `update-go_modules-graph` also report on `main` and are deliberately not
  required: the first is skipped on every run and the second is Dependabot's.

## Prerequisites

None. Every fix below is reachable from the working tree or from the GitHub REST
API with the `repo` scope the local `gh` token already holds: repository security
settings, rulesets, vulnerability alerts, and CodeQL default setup are all
writable through `gh api`. No registry form, password prompt, or web-only step
stands in front of any fix group.

## Verdicts

- R-DOC-01 pass README.md:3, one sentence before any heading or badge
- R-DOC-02 pass README.md:12-15 install block, README.md:33-42 usage block with its output, both before the first `##` at line 44
- R-DOC-03 pass README.md:136 links CHANGELOG.md, :137 links CONTRIBUTING.md, :147 links LICENSE; all three exist at the paths R-CHG-01, R-COM-02, and R-COM-01 accept
- R-DOC-04 pass README.md:73-82 command table matches the nine `case` arms in main.go:69-85; the 50ms claim at README.md:8 matches main_test.go:45; Go 1.26.6 matches go.mod:3; darwin and linux only matches .goreleaser.yml:11-16
- R-DOC-05 pass every em dash is gone from docs/agents/, and `## Considered Options` is `## Considered options` in all nine docs/adr/ files. prose.mjs reports nothing in scope: its ten CODE_OF_CONDUCT.md headings are exempt, attributed to Contributor Covenant 3.0 at line 157, and docs/plugin-verification.md:39 is a false positive, a sentence-case heading whose only capitals are the date and the proper noun Claude Code. Its remaining findings are out of the rule's scope: skills/add/SKILL.md:33,154 are not under docs/, and the six banned words at oss-audit-report.md:61 are this file quoting R-DOC-05's own Check line back. Do not re-fix that heading on the next audit: it reads `## 2026-08-18, v0.1.0-rc.1 on Claude Code, darwin/arm64`, and the linter counts the capitals in the version string and the harness name as Title Case
- R-DOC-06 pass README.md:129 names macOS, Linux, amd64, arm64; README.md:5,98 name Claude Code and Codex CLI; all appear in .goreleaser.yml and hooks/hooks.json
- R-DOC-07 pass all nine fenced blocks carry a language tag; the non-shell block at README.md:19 is preceded by "Save this as `.handrail/local/no-force-push.md`", and the two `text` blocks are command output adjacent to their `bash` block
- R-DOC-08 pass README.md:139 links https://github.com/svyatov/handrail/issues for both questions and bugs
- R-DOC-09 pass README.md:141, "handrail is maintained and pre-1.0"
- R-DOC-10 pass README.md:7 names hookify by name, README.md:8 states a 50ms median traceable to main_test.go:45, both before the first `##`
- R-COM-01 pass LICENSE at the root, MIT; .claude-plugin/plugin.json and .codex-plugin/plugin.json both declare `"license": "MIT"`
- R-COM-02 pass CONTRIBUTING.md:25-29 setup, :38-42 test, :55-64 pull request, :48 test-with-change, :6-15 names docs/spec.md and CLAUDE.md, both present
- R-COM-03 pass CODE_OF_CONDUCT.md:75, leonid@svyatov.com, no placeholder
- R-COM-04 pass SECURITY.md:5-6 GitHub private vulnerability reporting, enabled on this public repo; :11 commits to a first response within 14 days
- R-COM-05 pass .github/ISSUE_TEMPLATE/01-bug.yml and 02-proposal.yml; .github/pull_request_template.md
- R-COM-06 n/a one principal holds every merge path, so no eligible second owner exists
- R-COM-07 pass description, ten topics, and homepage https://pkg.go.dev/github.com/svyatov/handrail all set
- R-COM-08 pass CONTRIBUTING.md:79-84, sole maintainer named and no succession arranged, stated outright
- R-COM-09 n/a Discussions is disabled and the project runs no other public non-defect channel; README.md:139 routes questions and bugs to the same tracker
- R-CI-01 pass .github/workflows/ci.yml:3-6, push to `main` and `pull_request` with no `branches:` filter
- R-CI-02 pass CI runs `task build`, `task cover`, `task lint`, `task release-check`; CONTRIBUTING.md:38-42 documents `task test`, `task cover`, `task lint`, and `task test`'s check runs inside `task cover`
- R-CI-03 pass go.mod:3 declares 1.26.6 and both setup-go steps use `go-version-file: go.mod`; README declares no broader support range
- R-CI-04 pass actions/setup-go's default module cache keys on go.sum and separates by runner OS, architecture, and Go version; no other dependency cache is defined
- R-CI-05 pass collect.mjs reports `jobsWithTimeout: 4/4` (ci.yml:35,51,70 and release.yml:19); ci.yml:13 sets `concurrency` with `cancel-in-progress: true`. release.yml:12 sets `cancel-in-progress: false` on purpose: it publishes assets and must never be cancelled mid-upload, and it runs on a tag push rather than a change request, which is what the rule scopes the cancel to
- R-CI-06 pass testdata/script/*.txtar plus internal/ unit tests, run by `task test` and `task cover` in Taskfile.yml:31-44
- R-SEC-01 pass collect.mjs reports `externalUses: 15`, `externalUsesUnpinned: []`. Every line carries a 40-character SHA with its version tag in a trailing comment. The repository still ships no `action.yml` or `action.yaml`
- R-SEC-02 pass ci.yml:8-9 and release.yml:8-9 both set top-level `contents: read`; release.yml:20-21 raises `contents: write` at the job level only
- R-SEC-03 pass .github/dependabot.yml declares both ecosystems the project has: `gomod` at :12 for go.mod and go.sum, `github-actions` at :23 for the pins R-SEC-01 froze. Weekly, one grouped pull request each. No container images to cover
- R-SEC-04 pass ruleset 21093767, active on `~DEFAULT_BRANCH`, read back from `repos/svyatov/handrail/rulesets/21093767`: rules `pull_request`, `required_status_checks` with six contexts, `deletion`, and `non_fast_forward`, and `bypass_actors` empty, so the admin does not get a way around it either. `required_approving_review_count` is 0 because one principal holds every merge path, which is the same fact that makes R-COM-06 and R-SEC-12 not applicable; a count of 1 would leave nothing mergeable. `allowed_merge_methods` is squash only, matching CONTRIBUTING.md On GitLab, `GET /projects/:id/protected_branches/:name`.
- R-SEC-05 pass `git tag -v v0.1.0` reports a good signature for leonid@svyatov.com against the ED25519 key published at users/svyatov/ssh_signing_keys, and `git cat-file -t v0.1.0` prints `tag`
- R-SEC-06 n/a GitLab-only rule; this project runs on GitHub and ships no .gitlab-ci.yml
- R-SEC-07 pass the only `${{ }}` expression inside a `run:` is `${{ matrix.args }}` at ci.yml:40, which the workflow defines at :22,27; no `pull_request_target` or `workflow_run` workflow exists
- R-SEC-08 pass go.sum is committed and records module hashes; Go's default `-mod=readonly` fails on stale or missing resolution data and no CI step passes `-mod=mod`
- R-SEC-09 pass `code-scanning/default-setup` reports `state: configured`, languages `actions` and `go`, default query suite, weekly schedule; it runs on pull requests to `main`, and both check names it reports, `Analyze (go)` and `Analyze (actions)`, are required by ruleset 21093767
- R-SEC-10 pass `security_and_analysis` read back after the write reports `secret_scanning: enabled` and `secret_scanning_push_protection: enabled`. `secret_scanning_non_provider_patterns` and `secret_scanning_validity_checks` stay disabled; both are GitHub Secret Protection, which the rule puts outside itself
- R-SEC-11 pass go-modules: `vulnerability-alerts` answers 204 and `dependabot_security_updates` reports enabled, both read back after the write. GitHub parses go.mod and go.sum natively and lists `gomod` for security updates, so there is no residual for a second scanner to cover
- R-SEC-12 n/a `collaborators?affiliation=all` returns one principal, `svyatov`, holding every merge path
- R-SEC-13 pass two rulesets on `refs/tags/*`, both active, because a bypass actor is scoped to the ruleset rather than to a rule. 21093604 `Only an admin creates a release tag` holds `creation` alone with `RepositoryRole:5` bypassing it. 21094237 `Release tags never move` holds `update` and `deletion` with `bypass_actors: []`, and reads back `current_user_can_bypass: never`, so the sole admin cannot repoint or delete a tag either. A single ruleset holding all three rules does not satisfy this Check: the bypass that permits creation permits force-update and deletion with it. The project publishes no container image, which the rule puts outside itself anyway
- R-SEC-14 pass .github/dependabot.yml:16-17 and :26-27 set `cooldown.default-days: 7` on both ecosystems. Neither carries `include` or `exclude`, so the delay reaches every dependency, and 7 is above the three days Dependabot applies where nothing is written. It applies to version updates only and never delays a security update. This rule was not applicable on the first run, when the repository had no updater configuration to score
- R-SEC-15 n/a go-modules: nothing a Go dependency ships runs during resolution, which the rule names as outside it
- R-PUB-01 n/a go-modules takes the tag-published track, where the preamble drops R-PUB-01 through R-PUB-04 as a whole
- R-PUB-02 n/a go-modules takes the tag-published track, where the preamble drops R-PUB-01 through R-PUB-04 as a whole
- R-PUB-03 n/a go-modules takes the tag-published track, where the preamble drops R-PUB-01 through R-PUB-04 as a whole
- R-PUB-04 n/a go-modules takes the tag-published track, where the preamble drops R-PUB-01 through R-PUB-04 as a whole
- R-PUB-05 fail release v0.1.0 still carries four built `tar.gz` archives, `checksums.txt`, and no SBOM. The fix is in the working tree and verifies on the next tag: .goreleaser.yml:33-34 declares `sboms: - artifacts: archive`, and release.yml:33 installs syft, which GoReleaser shells out to. This rule scores a published release, so nothing closes it before that release exists
  Fixed by: oss-publish
  Check: a release that carries a built asset also carries a software bill of materials in SPDX or CycloneDX format, published as a release asset or at a path the README or release notes name, covering the dependencies that went into that asset. A release carrying only the source archives the forge generates falls outside this rule rather than failing it.
- R-PUB-06 fail `checksums.txt` on v0.1.0 is still unsigned and `repos/svyatov/handrail/attestations/...` still returns 404. The fix is in the working tree and verifies on the next tag: release.yml:54-58 runs `actions/attest-build-provenance` over `dist/*.tar.gz` and `dist/checksums.txt`, with `id-token: write` and `attestations: write` granted at :22-23. The attestation identity is the workflow itself, discoverable from the repository, and a downloader verifies with `gh attestation verify <file> --repo svyatov/handrail`, which release.yml:51-53 records. This rule scores a published release, so nothing closes it before that release exists
  Fixed by: oss-publish
  Check: each built asset on the newest release either carries a detached signature or appears with its cryptographic hash in a manifest that is itself signed, and the verifying key or attestation identity is discoverable from the repository rather than only from the release itself. A forge attestation covering the exact asset satisfies this. A release carrying only the source archives the forge generates falls outside this rule rather than failing it.
- R-PUB-07 n/a go-modules exposes no registry entry to update, which the rule names as outside it: proxy.golang.org fetches a tag on demand with no account and no credential
- R-CHG-01 pass CHANGELOG.md:1 `# Changelog`, :5 pinned Keep a Changelog 2.0.0 link, :11 `## [Unreleased]`, dated releases newest first at :13 and :49, `### Added` group at :18, comparison links at :55-57
- R-CHG-02 pass CHANGELOG.md:7-9 declares the public surface as the command set, the rule file format, and the exit codes; versions are valid SemVer with the `v` prefix on tags only; 0.y.z is handled explicitly
- R-CHG-03 pass tag v0.1.0, .claude-plugin/plugin.json and .codex-plugin/plugin.json both `"version": "0.1.0"`, CHANGELOG.md:13 `[0.1.0]`; scripts/check-version-pin.sh enforces it in `task release-check`
- R-CHG-04 pass `gh release view v0.1.0 --json body` returns the CHANGELOG.md `[0.1.0]` section verbatim, in place of the 30-subject commit list it carried before. Future tags get the same body without a second manual edit: release.yml:39-40 runs scripts/release-notes.sh over the tag name and passes the file to `goreleaser release --release-notes`, which skips GoReleaser's own changelog generation. `task release-check` runs the same script, so a tag whose changelog section is missing fails on a pull request rather than mid-release
- R-CHG-05 n/a the changelog carries no Removed group and the project has removed no public item
- R-CHG-06 n/a `go list -deps` reports zero non-stdlib runtime dependencies, `security-advisories` returns 0, and no release resolved a publicly known vulnerability
- R-CHG-07 pass go-modules encodes the major in package identity from v2 onward; the released major is 0, and go.mod:1 correctly carries no `/vN` suffix
- R-SKL-01 pass `skills/` at the root with `add/` and `analyze/` as direct children, each holding a SKILL.md; `git ls-files` finds no other SKILL.md and the repository commits no symlinked skill directory
- R-SKL-02 pass oss-skill's scripts/validate.mjs, run against the repository root, reports `0 error(s), 0 warning(s)`. On the first run its only two errors were the R-SKL-04 licence findings, now closed
- R-SKL-03 pass skills/add/SKILL.md 168 lines, skills/analyze/SKILL.md 142 lines
- R-SKL-04 pass skills/add/SKILL.md:4 and skills/analyze/SKILL.md:4 both carry `license: MIT`, matching the repository LICENSE that covers them. oss-skill's validate.mjs now reports `0 error(s), 0 warning(s)`
- R-SKL-05 n/a neither skill ships a `scripts/` directory, a dependency manifest, a lockfile, or `node_modules`
- R-SKL-06 pass README.md names Claude Code (:10) and Codex CLI (:98); .claude-plugin/plugin.json with .claude-plugin/marketplace.json and .codex-plugin/plugin.json are committed at the paths each host reads, and no third host is named
- R-SKL-07 pass both descriptions state the outcome and the trigger; both bodies are one numbered workflow with defaults (add: warn action, project-personal tier; analyze: project-personal, never project-shared), preserved gotchas (RE2 has no lookarounds, the transcript tail lags), and a verification loop (add steps 5 and 6, analyze step 6.2 and 6.3); analyze links `../add/SKILL.md` at the branch that needs it

## Execution order

Each group is a fresh session that reads this file and works from it, so no
handover is needed. A merged pull request is the checkpoint that lets the next
group start from a small context.

Groups 1 to 6 of the original seven ran together on one branch,
`chore/oss-audit-fixes`, and are closed. Only group 7 is left, and it is a
release rather than a fix.

1. ~~**oss-ci**, branch `ci/job-timeouts`, 1 rule: R-CI-05.~~ Done.
2. ~~**oss-harden**, branch `ci/harden`, 7 rules: R-SEC-01, R-SEC-03, R-SEC-04,
   R-SEC-09, R-SEC-10, R-SEC-11, R-SEC-13.~~ Done. R-SEC-14 flipped from not
   applicable to pass with it, because the cooldown landed in the same file.
3. ~~**oss-readme**, branch `docs/readme-links`, 1 rule: R-DOC-03.~~ Done.
4. ~~**oss-writing**, branch `docs/prose-style`, 1 rule: R-DOC-05.~~ Done.
5. ~~**oss-changelog**, branch `docs/release-notes`, 1 rule: R-CHG-04.~~ Done,
   by correcting the v0.1.0 release body in place.
6. ~~**oss-skill**, branch `docs/skill-license`, 1 rule: R-SKL-04.~~ Done.
7. **oss-publish**, branch `build/release-provenance`, 2 rules: R-PUB-05,
   R-PUB-06. Last, and it cuts the release. Both rules verify only against a
   newly published tag, so they stay open until the next `v*` tag runs the
   changed release workflow. That is the release closing them, not a fix that
   failed. The configuration is already in the tree; this group pushes the tag
   and then confirms:

   - `gh release view <tag> --json assets` lists one `.sbom.json` beside each
     `tar.gz`, which closes R-PUB-05.
   - `gh attestation verify handrail_<version>_darwin_arm64.tar.gz --repo svyatov/handrail`
     succeeds, which closes R-PUB-06.

   Two things about that release are worth knowing before it runs, because
   nothing in this repository has exercised them yet. Ruleset 21093604 now
   restricts tag creation to the admin role, so pushing the tag as `svyatov`
   works and a token that is not that principal does not. And `goreleaser check`
   never ran locally, because goreleaser is not installed on this machine; the
   `release-config` job runs it on every pull request, so the `sboms:` block is
   validated there before any tag exists.
